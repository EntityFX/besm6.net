<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="tests\**\*.cs" />
  </ItemGroup>

  <ItemGroup>
    <None Update="besm6.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

  <!-- SuperPlan Task A4: runtime-образы лент (monsys.9 / librar.12 / librar.37 / bemsh.739 / b.7)
       в этом репозитории git-ignored (см. .gitignore:35 tapes/) и намеренно НЕ встраиваются в
       dotnet publish: право на распространение подтверждено не было, пользователь предоставляет
       их сам. Checksum-manifest, порядок проверки и установка: docs/runtime-assets.md + класс
       Besm6.Loader.RuntimeAssets. Если право на переразрешение/распространение получено —
       добавьте <None Include="tapes\..." CopyToPublishDirectory="PreserveNewest" />. -->

  <ItemGroup>
    <PackageReference Include="Spectre.Console" Version="0.57.2" />
  </ItemGroup>
</Project>
