<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.7.0" />
    <PackageReference Include="MSTest" Version="4.0.2" />
    <PackageReference Include="MSTest.TestAdapter" Version="4.2.3" />
    <PackageReference Include="MSTest.TestFramework" Version="4.2.3" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Microsoft.VisualStudio.TestTools.UnitTesting" />
  </ItemGroup>

  <ItemGroup>
    <!-- Р“СЂР°РЅРёС‡РЅС‹Рµ РїСЂРѕРІРµСЂРєРё РІС‹РїРѕР»РЅСЏСЋС‚СЃСЏ С‡РµСЂРµР· СЂРµС„Р»РµРєСЃРёСЋ РЅР°Рґ СЃРѕР±СЂР°РЅРЅС‹РјРё СЃР±РѕСЂРєР°РјРё.
         РњРѕРЅРѕР»РёС‚РЅС‹Р№ executable РїРѕРґРєР»СЋС‡С‘РЅ, С‡С‚РѕР±С‹ РїСЂРѕРІРµСЂРєРё СЃСЃС‹Р»РѕРє РІС‹РїРѕР»РЅСЏР»РёСЃСЊ
         РІ С‚РѕРј Р¶Рµ РєРѕРЅС‚РµРєСЃС‚Рµ Р·Р°РіСЂСѓР·РєРё, С‡С‚Рѕ Рё СЃР°РјРѕ РїСЂРёР»РѕР¶РµРЅРёРµ. -->
    <ProjectReference Include="..\..\src\besm6.net\besm6.csproj" />
    <ProjectReference Include="..\..\src\Besm6.Architecture\Besm6.Architecture.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Besm6.Architecture" />
    <Using Include="Besm6.Runtime" />
  </ItemGroup>

</Project>
