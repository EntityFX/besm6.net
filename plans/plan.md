Я проанализировал предоставленный C++ код арифметического устройства (ALU) и сравнил его с текущей реализацией в C#. На данный момент C# версия реализует общую логику, но имеет значительные расхождения в деталях нормализации, округления, а также в алгоритмах умножения и деления.

Для приведения нашего кода в точное соответствие с эталоном, мне необходимо выполнить следующие шаги:

### План обновления АЛУ

1.  **Обновление `MantissaExponent`**:
    *   Скорректировать конструктор для обеспечения идентичного расширения знака (sign extension) при создании объекта из `Word48`.
    *   Убедиться, что методы `Negate()` и `IsDenormal()` работают точно так же, как в C++.
    *   Реализовать метод `Multiply`, в точности повторяющий C++ версию `MantissaExponent::multiply` (с ручным вычислением произведения через частичные произведения для 40-битных чисел).

2.  **Реализация `ArithAdd` (Сложение/Вычитание)**:
    *   Полностью переписать метод `ArithAdd`, чтобы он соответствовал `Processor::arith_add`.
    *   Реализовать правильную обработку четырех вариантов операции: обычное сложение, вычитание, обратное вычитание и вычитание модулей.
    *   Точно реализовать логику выравнивания порядков с сохранением остатка в `mr` и установкой `round_flag`.

3.  **Реализация `ArithNormalizeAndRound` (Нормализация и округление)**:
    *   Это критический узел. Я заменю текущую логику на алгоритм из `Processor::arith_normalize_and_round`.
    *   Реализую обработку случаев `i == 0` (нормализация влево) и `i == 3` (нормализация вправо/инверсия), используя расчет сдвига `cnt = 39 - HighestBit(r)`.
    *   Обновлю логику установки итогового значения в `regs.A` и `regs.RMR`, включая обработку переполнения (Overflow).

4.  **Обновление умножения (`ArithMultiply`)**:
    *   Обновлю `ArithMultiply`, чтобы он использовал новый метод `Multiply` и корректно рассчитывал итоговый порядок.

5.  **Обновление деления (`ArithDivide`)**:
    *   Заменю текущую реализацию на базе `BigInteger` на алгоритм невосстанавливающего деления (`nrdiv`), как в эталоне.
    *   Это обеспечит идентичное поведение БЭСМ-6 при делении, включая специфические случаи с нормализацией.

6.  **Обновление сдвигов (`ArithShift`)**:
    *   Приведу логику сдвигов в соответствие с `Processor::arith_shift`, особенно в части работы с `RMR` при сдвигах более 48 бит.

После реализации этих изменений я проведу тестирование с использованием `AluTests.cs`, чтобы убедиться, что результаты операций полностью совпадают с ожидаемыми.

Если вы согласны с этим планом, пожалуйста, **переключите меня в ACT MODE**, чтобы я мог приступить к правкам кода.